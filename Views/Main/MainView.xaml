<Window x:Name="Main" x:Class="PGMLab.Views.Main.MainView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d"
		xmlns:MaterialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" TextElement.Foreground="{DynamicResource MaterialDesignBody}" TextElement.FontWeight="Regular" TextElement.FontSize="13" TextOptions.TextFormattingMode="Ideal" TextOptions.TextRenderingMode="Auto" Background="Transparent" FontFamily="{DynamicResource MaterialDesignFont}" AllowsTransparency="True"
		WindowStyle="None" WindowStartupLocation="CenterScreen"
		xmlns:Main="clr-namespace:PGMLab.ViewModels.Main">
	<Window.DataContext>
		<Main:MainViewModel/>
	</Window.DataContext>
	<Window.InputBindings>
		<KeyBinding Command="{Binding TestCommand}" Modifiers="Ctrl" Key="T"/>
	</Window.InputBindings>
	<Window.Style>
		<Style TargetType="Window">
			<Style.Setters>
				<Setter Property="Background" Value="Transparent"/>
				<Setter Property="Title" Value="Ant Colony"/>
				<Setter Property="MinHeight" Value="720"/>
				<Setter Property="MinWidth" Value="1280"/>
				<Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
				<Setter Property="WindowStyle" Value="ThreeDBorderWindow"/>
			</Style.Setters>
		</Style>
	</Window.Style>
	<DockPanel LastChildFill="True" Margin="4">
		<MaterialDesign:Card Background="{DynamicResource MaterialDesignDarkBackground}" DockPanel.Dock="Top" Panel.ZIndex="1024" Padding="8, 6, 8, 0">
			<Grid DockPanel.Dock="Top" Background="Transparent" MouseDown="CheckMouseDown" HorizontalAlignment="Stretch">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="3*"/>
					<ColumnDefinition Width="10*"/>
					<ColumnDefinition Width="3*"/>
				</Grid.ColumnDefinitions>
				<StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Grid.Column="0">
					<Menu Style="{DynamicResource MaterialDesignMenu}">
						<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_File">
							<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_New"/>
							<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_Open..."/>
							<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_Save"/>
							<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_Save as..."/>
							<MenuItem Style="{DynamicResource MaterialDesignMenuItem}" Header="_Exit" Click="Exit"/>
						</MenuItem>
					</Menu>
				</StackPanel>
				<TextBlock Grid.Column="1" Foreground="{DynamicResource MaterialDesignDarkForeground}" Style="{DynamicResource MaterialDesignBody1TextBlock}" HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding Title, ElementName=Main}"/>
				<StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
					<Button Click="Minimize" Foreground="{DynamicResource MaterialDesignDarkForeground}" Style="{DynamicResource MaterialDesignIconButton}" HorizontalAlignment="Center" VerticalAlignment="Center">
						<MaterialDesign:PackIcon Kind="WindowMinimize"/>
					</Button>
					<Button Click="Maximize" Foreground="{DynamicResource MaterialDesignDarkForeground}" Style="{DynamicResource MaterialDesignIconButton}" HorizontalAlignment="Center" VerticalAlignment="Center">
						<MaterialDesign:PackIcon Kind="{Binding MaximizeIcon, ElementName=Main, TargetNullValue=WindowMaximize}"/>
					</Button>
					<Button Click="Exit" Foreground="{DynamicResource MaterialDesignDarkForeground}" Style="{DynamicResource MaterialDesignIconButton}" HorizontalAlignment="Center" VerticalAlignment="Center">
						<MaterialDesign:PackIcon Kind="Close"/>
					</Button>
				</StackPanel>
			</Grid>
		</MaterialDesign:Card>
		<MaterialDesign:Card></MaterialDesign:Card>
	</DockPanel>
</Window>
